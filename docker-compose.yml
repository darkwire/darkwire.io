version: '2'

services:
    dwserver:
        image: darkwire/darkwire-server:2.0.0-beta2
        environment:
            - PROTOCOL=http
            - PORT=8080
        ports:
            - "8080:8080"
    dwclient:
        image: darkwire/darkwire-client:2.0.0-beta2
        environment:
            - NODE_ENV=production
            - API_SERVER=dwserver
            - PROTOCOL=http
            - PORT=3000
        ports:
            - "80:80"
